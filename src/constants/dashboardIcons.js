import { Activity, History, LayoutDashboard, Thermometer, AudioWaveform, Zap } from 'lucide-react'

export const navigationIcons = [LayoutDashboard, Activity, History]

export const sensorIcons = [Thermometer, Activity, AudioWaveform, Zap]
