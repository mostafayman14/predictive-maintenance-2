VITE_API_BASE_URL=/api/v1
VITE_API_PROXY_TARGET=http://localhost:8000
# Set to true to poll POST /live on an interval. Required for live charts.
VITE_LIVE_POLLING_ENABLED=true
# Interval (ms) between live POST /live polls when enabled. Defaults to 1000.
VITE_LIVE_POLL_INTERVAL=1000
